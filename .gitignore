# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local
.env.production
bot/.env

# bot
bot/node_modules

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code
.claude/


# Bot runtime data (PII — never commit)
bot/data/

# Stray nested project copy (has its own .git — never commit)
/ai-academy/

# Learning materials (large, local-only — never commit to public repo)
/Фронтир - обучение/
