FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app
COPY . /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends assimp-utils \
    && rm -rf /var/lib/apt/lists/*

CMD ["python", "deploy/fbx_converter.py"]